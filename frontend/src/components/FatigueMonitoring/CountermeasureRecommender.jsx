 import React from "react";

const CountermeasureRecommender = ({ recommendation }) => (
  <div>
    <h4>Recommended Countermeasure</h4>
    <p>{recommendation}</p>
  </div>
);

export default CountermeasureRecommender;
